# Land Characterization System Documentation (Full Build)

Open `index.html` in your browser. After editing or adding pages, rebuild search with:
```bash
npm install jsdom
node build_search_index.js
```

Replace placeholder figures by swapping `<figure class="placeholder">...</figure>` with `<img src="assets/images/your_image.png" alt="">`.
